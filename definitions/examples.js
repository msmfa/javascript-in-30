//Examples used:

//functions

//functional expression

const square = function (number) {
  return number * number;
};

class Rectangle {
  constructor(height, width) {
    this.height = height;
    this.width = width;
  }
}
